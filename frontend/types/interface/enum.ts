export enum AuthorityRole {
  ADMIN = 'ADMIN',
  MANAGER = 'MANAGER',
  USER = 'USER',
  ANONYMOUS = 'ANONYMOUS',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
  ORTHER = 'orther',
}

export enum UserStatus {
  active = 'active',
  block = 'block',
}
